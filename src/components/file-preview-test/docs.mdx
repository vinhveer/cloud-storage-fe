import FilePreviewTestPage from '@/api/features/page/page-file/file-preview-test.page'

## File preview test

Trang này dùng để test API preview file (`GET /api/files/{id}/preview`).

- Nhập `File ID` rồi nhấn **Fetch preview**.
- Hoặc dùng các nút **Quick** để test nhanh PDF, TXT, và forbidden.

> Nhớ đăng nhập trước để có token hợp lệ.

<FilePreviewTestPage />


