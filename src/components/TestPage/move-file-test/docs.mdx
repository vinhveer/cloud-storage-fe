import MoveFileTestPage from '@/api/features/page/page-file/move-file-test.page'

## Move file test

Trang này dùng để test API move file (`POST /api/files/{id}/move`).

- Nhập `File ID` và (optional) `Destination Folder ID`, sau đó nhấn **Move file**.
- Hoặc dùng các nút **Quick** để test nhanh các case trong spec.

> Nhớ đăng nhập trước để có token hợp lệ.

<MoveFileTestPage />


