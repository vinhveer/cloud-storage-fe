import FolderTreeTestPage from '@/api/features/page/page-folder/folder-tree-test.page'

## Folder tree test

Trang này dùng để test API **GET /api/folders/tree** (lấy toàn bộ cây thư mục của người dùng).

- Bấm **Fetch folder tree** để gọi API và xem JSON nested ở phần Result.
- Nếu bạn logout hoặc dùng token hết hạn, bấm lại nút sẽ trả về lỗi 401 (`UNAUTHENTICATED`).

<FolderTreeTestPage />



