import AdminConfigUpdateTestPage from '@/api/features/page/admin-config-update-test.page'

## Admin config update test

Trang này dùng để test API cập nhật cấu hình (`PUT /api/admin/configs/{key}`).

Nhớ đăng nhập trước để có token hợp lệ.

<AdminConfigUpdateTestPage />


