import AdminConfigDetailTestPage from '@/api/features/page/admin-config-detail-test.page'

## Admin config detail test

Trang này dùng để test API lấy thông tin cấu hình chi tiết (`GET /api/admin/configs/{key}`).

> Nhớ đăng nhập trước để có token hợp lệ.


<AdminConfigDetailTestPage />


