import AdminStorageUsersTestPage from '@/api/features/page/admin-storage-users-test.page'

## Admin storage users test

Trang này dùng để test API danh sách dung lượng theo user (`GET /api/admin/storage/users`).

- Có thể nhập `search`, `page`, `per_page`.
- Nhấn **Fetch users storage** để lấy dữ liệu.
- Quick: mặc định, search, phân trang, hướng dẫn 401, hướng dẫn unauthorized.

> Yêu cầu quyền admin để xem dữ liệu.

<AdminStorageUsersTestPage />



