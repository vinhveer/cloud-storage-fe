import AdminConfigsTestPage from '@/api/features/page/admin-configs-test.page'

## Admin configs test

Trang này dùng để test API lấy danh sách cấu hình hệ thống (`GET /api/admin/configs`).

- Nhập tham số và nhấn **Fetch configs**.
- Hoặc dùng các nút **Quick** để test nhanh các case trong spec.

> Nhớ đăng nhập trước để có token hợp lệ.


<AdminConfigsTestPage />


