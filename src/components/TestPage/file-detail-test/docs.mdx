import FileDetailTestPage from '@/api/features/page/page-file/file-detail-test.page'

## File detail test

Trang này dùng để test API lấy chi tiết file (`GET /api/files/{id}`).

- Nhập `File ID` rồi nhấn **Fetch detail**.
- Hoặc dùng các nút **Quick** để test nhanh các case trong spec.

> Nhớ đăng nhập trước để có token hợp lệ.

<FileDetailTestPage />


