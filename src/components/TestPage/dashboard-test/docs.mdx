import DashboardTestPage from '@/api/features/page/page-dashboard/dashboard-test.page'

## Dashboard overview test

Trang này dùng để test API dashboard (`GET /api/dashboard`).

- Nhấn **Fetch dashboard** để gọi API và xem body.

> Nhớ đăng nhập trước để có token hợp lệ.

<DashboardTestPage />


