import FolderDeleteTestPage from '@/api/features/page/page-folder/folder-delete-test.page'

## Folder delete test

Trang này dùng để test API **DELETE /api/folders/\{id\}** (xoá mềm folder, chuyển vào Trash).

- Nhập `Folder ID` rồi nhấn **Delete folder**.
- Hoặc dùng các nút **Quick** để test nhanh các case: 200, 404, 403, 401.

> Nhớ đăng nhập trước để có token hợp lệ (trừ khi bạn cố tình test 401).

<FolderDeleteTestPage />



