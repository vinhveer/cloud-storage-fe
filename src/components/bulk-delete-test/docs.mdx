import BulkDeleteTestPage from '@/api/features/page/page-bulk/bulk-delete-test.page'

## Bulk delete test

Trang này dùng để test API bulk delete (`POST /api/bulk/bulk-delete`).

- Nhập `file_ids`, `folder_ids` rồi nhấn **Run bulk delete**.
- Hoặc dùng các nút **Quick** để test nhanh happy path, validation fail và unauthenticated.

> Nhớ đăng nhập trước để có token hợp lệ.

<BulkDeleteTestPage />


