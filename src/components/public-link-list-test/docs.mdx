import PublicLinkListTestPage from '@/api/features/page/page-public-link/public-link-list-test.page'

## Public link list test

Trang này dùng để test API **GET /api/public-links** (danh sách tất cả public link của user hiện tại).

- Nhập `page` và `per_page` rồi bấm **Fetch public links**.
- Hoặc dùng các nút **Quick** để test nhanh các case: thành công (200) và unauthenticated (401).

> Nhớ đăng nhập trước để có token hợp lệ (trừ khi test case 401 trong tab chưa đăng nhập).

<PublicLinkListTestPage />



