import AdminUsersTestPage from '@/api/features/page/admin-users-test.page'

## Admin users test

Trang này dùng để test API danh sách tất cả người dùng (`GET /api/admin/users`).

- Có thể nhập `search`, `page`, `per_page`.
- Nhấn **Fetch users** để lấy dữ liệu.
- Quick: mặc định, search, phân trang, hướng dẫn 401, hướng dẫn unauthorized.

> Yêu cầu quyền admin để xem dữ liệu.

<AdminUsersTestPage />



