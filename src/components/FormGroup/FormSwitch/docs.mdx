import React from 'react'
import FormSwitch from '@/components/FormGroup/FormSwitch/FormSwitch'

# FormSwitch

Nút gạt (switch). Label/help/error được cung cấp bởi `FormGroup`.

## Mục đích & trường hợp xử lý

- Bật/tắt một tùy chọn dạng boolean trong form.

## Props

| Tên | Kiểu | Mặc định | Mô tả |
| --- | --- | --- | --- |
| checked | `boolean` | - | Trạng thái controlled |
| defaultChecked | `boolean` | - | Trạng thái ban đầu (uncontrolled) |
| onCheckedChange | `(checked: boolean) => void` | - | Callback khi đổi trạng thái |
| className | `string` | - | Bổ sung class Tailwind |
| ...rest | `React.ButtonHTMLAttributes<HTMLButtonElement>` | - | Thuộc tính button gốc |

## Cách dùng
```tsx
<FormGroup label="Kích hoạt" help="Có thể thay đổi bất kỳ lúc nào">
  <FormSwitch defaultChecked />
</FormGroup>
```

### Demos

```tsx
import FormSwitch from '@/components/FormGroup/FormSwitch/FormSwitch'
```

#### Cơ bản

```tsx
<>
  <FormSwitch label="Nhận thông báo" help="Bạn có thể tắt mở bất kỳ lúc nào" />
  <FormSwitch label="Mặc định bật" defaultChecked />
</>
```

<div className="grid gap-4">
  <FormSwitch label="Nhận thông báo" help="Bạn có thể tắt mở bất kỳ lúc nào" />
  <FormSwitch label="Mặc định bật" defaultChecked />
</div>

#### Controlled

```tsx
{(() => {
  const [on, setOn] = React.useState(false)
  return (
    <FormSwitch label={on ? 'Đang bật' : 'Đang tắt'} checked={on} onCheckedChange={setOn} help="Ví dụ controlled" />
  )
})()}
```

{(() => {
  const [on, setOn] = React.useState(false)
  return (
    <div className="grid gap-4">
      <FormSwitch label={on ? 'Đang bật' : 'Đang tắt'} checked={on} onCheckedChange={setOn} help="Ví dụ controlled" />
    </div>
  )
})()}

## Lưu ý

- Controlled: truyền `checked` + `onCheckedChange`. Uncontrolled: dùng `defaultChecked`.
- `aria-pressed` phản ánh trạng thái để hỗ trợ a11y.