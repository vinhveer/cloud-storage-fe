import UploadFileTestPage from '@/api/features/page/upload-file-test.page'

## File upload test

Trang này dùng để test API upload file (`POST /api/files`).

- Nếu để trống Folder ID: upload vào root (folder_id = null).
- Nếu nhập Folder ID: upload vào folder tương ứng.

> Nhớ đăng nhập trước để có token hợp lệ, vì endpoint yêu cầu Bearer token.

<UploadFileTestPage />


