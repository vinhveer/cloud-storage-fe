import FileDeleteTestPage from '@/api/features/page/file-delete-test.page'

## File delete test

Trang này dùng để test API xoá mềm file (`DELETE /api/files/{id}`).

- Nhập `File ID` rồi nhấn **Delete file**.
- Hoặc dùng các nút **Quick** để test nhanh các case trong spec.

> Nhớ đăng nhập trước để có token hợp lệ.

<FileDeleteTestPage />


