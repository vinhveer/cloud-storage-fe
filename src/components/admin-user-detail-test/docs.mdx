import AdminUserDetailTestPage from '@/api/features/page/admin-user-detail-test.page'

## Admin user detail test

Trang này dùng để test API lấy chi tiết 1 người dùng (`GET /api/admin/users/{id}`).

- Nhập `User ID` và nhấn **Fetch user**.
- Quick: success (200), not found (404), hướng dẫn 401, hướng dẫn unauthorized (500).

> Yêu cầu quyền admin để xem dữ liệu.

<AdminUserDetailTestPage />



