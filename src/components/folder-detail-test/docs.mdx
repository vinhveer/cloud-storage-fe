import FolderDetailTestPage from '@/api/features/page/page-folder/folder-detail-test.page'

## Folder detail test

Trang này dùng để test API **GET /api/folders/\{id\}** (lấy thông tin chi tiết một thư mục).

- Nhập `Folder ID` rồi bấm **Fetch folder detail** để xem chi tiết (bao gồm `is_deleted`, `deleted_at`).
- Các nút **Quick**: happy path (200), not found (404), unauthenticated (401), forbidden (403).

<FolderDetailTestPage />



