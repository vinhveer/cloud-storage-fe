import ListFilesTestPage from '@/api/features/page/list-files-test.page'

## List files test

Trang này dùng để test API lấy danh sách files (`GET /api/files`).

- Nhập các filter rồi nhấn **Fetch files**.
- Hoặc dùng các nút **Quick** để test nhanh các case trong spec.

> Nhớ đăng nhập trước để có token hợp lệ.

<ListFilesTestPage />


