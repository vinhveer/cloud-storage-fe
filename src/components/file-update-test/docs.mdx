import FileUpdateTestPage from '@/api/features/page/page-file/file-update-test.page'

## File update test

Trang này dùng để test API cập nhật file (`PUT /api/files/{id}`).

- Nhập `File ID`, `Display name`, `Folder ID` rồi nhấn **Update file**.
- Hoặc dùng các nút **Quick** để test nhanh các case trong spec.

> Nhớ đăng nhập trước để có token hợp lệ.

<FileUpdateTestPage />


