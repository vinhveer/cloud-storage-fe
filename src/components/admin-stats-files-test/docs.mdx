import AdminStatsFilesTestPage from '@/api/features/page/admin-stats-files-test.page'

## Admin file stats test

Trang này dùng để test API thống kê files (`GET /api/admin/stats/files`).

- Không yêu cầu nhập tham số.
- Nhấn **Fetch stats** để lấy dữ liệu.
- Để test 401 (Unauthenticated) hãy đăng xuất trước khi bấm nút.

> Nhớ đăng nhập bằng tài khoản có quyền admin để nhận dữ liệu 200.

<AdminStatsFilesTestPage />



