import { Subnav } from '@/components/Subnav/Subnav'

# Subnav

Thanh điều hướng con (pills) tái sử dụng `NavGroup`, hỗ trợ ngang/dọc, trạng thái active, icon class và badge.

## Props

| Tên | Kiểu | Mặc định | Mô tả |
| --- | --- | --- | --- |
| items | `SubnavItem[]` | `[]` | Danh sách mục điều hướng |
| activeItem | `string | null` | `null` | Mục đang active; khớp theo `id` hoặc `label` |
| orientation | `'horizontal' | 'vertical'` | `'horizontal'` | Hướng hiển thị |
| className | `string` | - | Bổ sung class Tailwind |
| ...rest | `React.HTMLAttributes<HTMLElement>` | - | Thuộc tính gốc cho `<nav>` |

## Cách dùng
```tsx
<Subnav
  items=[
    { id: 'home', label: 'Home', href: '#' },
    { id: 'files', label: 'Files', href: '#' },
    { id: 'settings', label: 'Settings', href: '#' },
  ]
  activeItem="files"
/>
```

### Demos

```tsx
import { Subnav } from '@/components/Subnav/Subnav'
```

#### Cơ bản

```tsx
{(() => {
  const items = [
    { id: 'home', label: 'Home', href: '#', icon: 'fas fa-home' },
    { id: 'files', label: 'Files', href: '#', icon: 'far fa-folder' },
    { id: 'settings', label: 'Settings', href: '#', icon: 'fas fa-cog' },
  ]
  return <Subnav items={items} activeItem="files" />
})()}
```

{(() => {
  const items = [
    { id: 'home', label: 'Home', href: '#', icon: 'fas fa-home' },
    { id: 'files', label: 'Files', href: '#', icon: 'far fa-folder' },
    { id: 'settings', label: 'Settings', href: '#', icon: 'fas fa-cog' },
  ]
  return <div className="mt-4"><Subnav items={items} activeItem="files" /></div>
})()}

#### Dọc

```tsx
{(() => {
  const items = [
    { id: 'overview', label: 'Overview', href: '#', icon: 'far fa-clipboard' },
    { id: 'usage', label: 'Usage', href: '#', icon: 'far fa-chart-bar' },
    { id: 'billing', label: 'Billing', href: '#', icon: 'far fa-credit-card' },
  ]
  return <Subnav items={items} activeItem="overview" orientation="vertical" />
})()}
```

{(() => {
  const items = [
    { id: 'overview', label: 'Overview', href: '#', icon: 'far fa-clipboard' },
    { id: 'usage', label: 'Usage', href: '#', icon: 'far fa-chart-bar' },
    { id: 'billing', label: 'Billing', href: '#', icon: 'far fa-credit-card' },
  ]
  return <div className="mt-4"><Subnav items={items} activeItem="overview" orientation="vertical" /></div>
})()}

#### Badge & icon

```tsx
{(() => {
  const items = [
    { id: 'inbox', label: 'Inbox', href: '#', badge: 5, icon: 'fas fa-inbox' },
    { id: 'shared', label: 'Shared', href: '#', badge: 2, icon: 'fas fa-share-alt' },
    { id: 'archive', label: 'Archive', href: '#', icon: 'fas fa-archive' },
  ]
  return <Subnav items={items} activeItem="shared" />
})()}
```

{(() => {
  const items = [
    { id: 'inbox', label: 'Inbox', href: '#', badge: 5, icon: 'fas fa-inbox' },
    { id: 'shared', label: 'Shared', href: '#', badge: 2, icon: 'fas fa-share-alt' },
    { id: 'archive', label: 'Archive', href: '#', icon: 'fas fa-archive' },
  ]
  return <div className="mt-4"><Subnav items={items} activeItem="shared" /></div>
})()}
