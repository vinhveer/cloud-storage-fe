import DashboardStatsTestPage from '@/api/features/page/page-dashboard/dashboard-stats-test.page'

## Dashboard stats test

Trang này dùng để test API thống kê dashboard (`GET /api/dashboard/stats`).

- Nhập `start_date`, `end_date` (định dạng `Y-m-d`) rồi nhấn **Fetch stats**.
- Hoặc dùng các nút **Quick** để test nhanh: range đầy đủ, chỉ start_date, chỉ end_date, và case invalid date.

> Nhớ đăng nhập trước để có token hợp lệ.

<DashboardStatsTestPage />


